{{!< default}}

{{!-- This is a page template. A page outputs content just like any other post, and has all the same
    attributes by default, but you can also customise it to behave differently if you prefer. --}}

{{!-- Everything inside the #post tags pulls data from the page --}}
{{#post}}

{{#if feature_image}}<style>.header .header-logo {background-image: url({{img_url feature_image}});background-color: #4A4A4A;background-repeat: no-repeat;background-position: center center;background-size: cover;}</style>{{/if}}

        <div class="content">
            <div class="article">
                {{content}}

                <hr>

                <div class="back-to-top">
                    <i class="fa fa-calendar"></i>&nbsp; {{date format="YYYY-MM-DD"}}  &nbsp;<i class="fa fa-chevron-circle-up"></i>&nbsp; <a href="#start">返回顶部</a>
                </div>
                
                <hr>

                <div id="comments">
                    <div class="ds-thread" data-thread-key="{{slug}}" data-title="{{title}}" data-url="{{url absolute="true"}}"></div>
                </div>
            </div>
        </div>

{{/post}}
